# Verify Unified (Public)

Public verifier + proof-format reference implementation.

- Cloudflare Worker: workers/verify-unified/src/index.mjs
- No secrets are committed. Configure deploy secrets in GitHub Actions.
