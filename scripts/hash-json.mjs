import fs from "fs"; import crypto from "crypto";  // Minimal RFC8785-style canonicalize: sort object keys, preserve array order function canon(v){   if (v === null || typeof v !== "object") return JSON.stringify(v);   if (Array.isArray(v)) return "[" + v.map(canon).join(",") + "]";   const keys = Object.keys(v).sort();   return "{" + keys.map(k => JSON.stringify(k) + ":" + canon(v[k])).join(",") + "}"; }  function hashBase64Url(str){   return crypto.createHash("sha256").update(str).digest("base64")     .replace(/\+/g,"-").replace(/\//g,"_").replace(/=+$/,""); }  const inPath = process.argv[2]; if (!inPath) { console.error("usage: node scripts/hash-json.mjs <file.json>"); process.exit(1); }  const raw = fs.readFileSync(inPath, "utf8").replace(/^\uFEFF/,""); const obj = JSON.parse(raw); const c   = canon(obj); const h   = hashBase64Url(c);  console.log(h);     
